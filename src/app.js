import { reactive } from 'vue'

function getTableData(params) {

  const tableData = reactive(defaults) // OBJECT

  const defaults = {

  }

  return tableData
}

function getMatch() {

}

function getResult() {

}